# Elite Insights CLI config — verified against the project's own
# GW2EIParserCommons/Settings/sample.conf. Only JSON output is needed; HTML/CSV
# generation is skipped to keep parsing fast and disk usage low.
SaveOutJSON=true
SaveOutHTML=false
SaveOutCSV=false
SaveOutTrace=false
SaveAtOut=true
IndentJSON=false

# Place `json.hpp` here

Download the single-header [nlohmann/json](https://github.com/nlohmann/json/releases)
(`json.hpp`) into this folder so the include resolves:

```
#include <nlohmann/json.hpp>
```

It is intentionally **not** committed (see `../../.gitignore`).
